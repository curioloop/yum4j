# SciPy Test Resources

This directory is reserved for raw SciPy-derived resources that Java tests cannot express compactly in code.

Correlation parity tests now use JUnit `MethodSource` tables transcribed from upstream SciPy test cases, so no generated JSON oracle is required here.