<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.curioloop</groupId>
    <artifactId>yum4j</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <packaging>jar</packaging>

    <name>YUM4J</name>
    <url>https://github.com/curioloop/yum4j</url>
    <description>Yet Useful Math for Java: scientific computing without the ceremony</description>

    <licenses>
        <license>
            <name>The Apache License, Version 2.0</name>
            <url>http://www.apache.org/licenses/LICENSE-2.0.txt</url>
        </license>
    </licenses>

    <developers>
        <developer>
            <name>CurioLoop</name>
            <email>curioloops@gmail.com</email>
            <organization>CurioLoop</organization>
            <organizationUrl>https://github.com/curioloop</organizationUrl>
        </developer>
    </developers>

    <scm>
        <connection>scm:git:git://github.com/curioloop/yum4j.git</connection>
        <developerConnection>scm:git:ssh://github.com/curioloop/yum4j.git</developerConnection>
        <url>https://github.com/curioloop/yum4j/tree/main</url>
    </scm>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <java.version>27</java.version>
        <maven.compiler.source>${java.version}</maven.compiler.source>
        <maven.compiler.target>${java.version}</maven.compiler.target>
        <maven.compiler.release>${java.version}</maven.compiler.release>
        <vector.module>jdk.incubator.vector</vector.module>
        <junit.version>5.10.1</junit.version>
        <jqwik.version>1.8.2</jqwik.version>
        <jackson.version>2.17.2</jackson.version>
        <gpg.skip>true</gpg.skip>
        <gpg.keyname>DB6E1C652E8FE9F5</gpg.keyname>
    </properties>

    <dependencies>
        <!-- JUnit 5 -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-api</artifactId>
            <version>${junit.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-engine</artifactId>
            <version>${junit.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-params</artifactId>
            <version>${junit.version}</version>
            <scope>test</scope>
        </dependency>
        <!-- jqwik for property-based testing -->
        <dependency>
            <groupId>net.jqwik</groupId>
            <artifactId>jqwik</artifactId>
            <version>${jqwik.version}</version>
            <scope>test</scope>
        </dependency>
        <!-- AssertJ for fluent assertions -->
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <version>3.24.2</version>
            <scope>test</scope>
        </dependency>
        <!-- JSON fixture loading for parity tests -->
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
            <version>${jackson.version}</version>
            <scope>test</scope>
        </dependency>
        <!-- JMH for benchmarking -->
        <dependency>
            <groupId>org.openjdk.jmh</groupId>
            <artifactId>jmh-core</artifactId>
            <version>1.37</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.openjdk.jmh</groupId>
            <artifactId>jmh-generator-annprocess</artifactId>
            <version>1.37</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <source>${java.version}</source>
                    <target>${java.version}</target>
                    <release>${maven.compiler.release}</release>
                    <parameters>true</parameters>
                    <proc>none</proc>
                    <compilerArgs>
                        <arg>--enable-preview</arg>
                        <arg>--add-modules</arg>
                        <arg>${vector.module}</arg>
                    </compilerArgs>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.5.2</version>
                <configuration>
                    <argLine>--enable-preview --add-modules ${vector.module}</argLine>
                    <includes>
                        <include>**/*Test.java</include>
                        <include>**/*Properties.java</include>
                    </includes>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <version>3.3.0</version>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-source-plugin</artifactId>
                <version>3.3.0</version>
                <executions>
                    <execution>
                        <id>attach-sources</id>
                        <goals><goal>jar-no-fork</goal></goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-javadoc-plugin</artifactId>
                <version>3.6.3</version>
                <configuration>
                    <doclint>none</doclint>
                    <additionalJOptions>
                        <additionalJOption>--enable-preview</additionalJOption>
                        <additionalJOption>--add-modules</additionalJOption>
                        <additionalJOption>${vector.module}</additionalJOption>
                    </additionalJOptions>
                </configuration>
                <executions>
                    <execution>
                        <id>attach-javadocs</id>
                        <goals><goal>jar</goal></goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-gpg-plugin</artifactId>
                <version>3.1.0</version>
                <executions>
                    <execution>
                        <id>sign-artifacts</id>
                        <phase>verify</phase>
                        <goals><goal>sign</goal></goals>
                        <configuration>
                            <keyname>${gpg.keyname}</keyname>
                            <passphraseServerId>${gpg.keyname}</passphraseServerId>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.sonatype.central</groupId>
                <artifactId>central-publishing-maven-plugin</artifactId>
                <version>0.4.0</version>
                <extensions>true</extensions>
                <configuration>
                    <publishingServerId>central</publishingServerId>
                    <tokenAuth>true</tokenAuth>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <version>3.1.0</version>
                <configuration>
                    <executable>java</executable>
                    <mainClass>org.openjdk.jmh.Main</mainClass>
                    <classpathScope>test</classpathScope>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <!--
            JMH benchmark profile for particle-v2-perf (and any other JMH-driven work).

            Activation:
                mvn -Pjmh test-compile                                          (compile only)
                mvn -Pjmh test-compile exec:exec@jmh-benchmark -Dbenchmarks=<regex>   (run)

            What this profile adds on top of the default build:
              1. Registers src/test/jmh as an additional test source root so benchmark
                 classes for this spec can live outside the regular unit-test tree.
              2. Enables JMH's annotation processor for test compilation so @Benchmark
                 classes produce META-INF/BenchmarkList (required by org.openjdk.jmh.Main).
                 The default pom sets <proc>none</proc> globally to keep fast test compiles;
                 this profile overrides that only for default-testCompile.
              3. Binds an execution of exec-maven-plugin (exec:exec → java →
                 org.openjdk.jmh.Main) that forwards -Dbenchmarks=<regex> to JMH's
                 benchmark-name filter. exec:exec is required (not exec:java) because
                 JMH forks a fresh JVM per benchmark and the forked VM needs its own
                 classpath.

            Note: jmh-core and jmh-generator-annprocess dependencies remain at top-level
            test scope because existing benchmarks outside this spec (src/test/java/.../stats,
            math, fft) depend on them at compile time. Placing them only inside this profile
            would break default test-compile.
        -->
        <profile>
            <id>jmh</id>

            <properties>
                <!-- Default: match all benchmarks. Override with -Dbenchmarks=<regex>. -->
                <benchmarks>.*</benchmarks>
            </properties>

            <dependencies>
                <!--
                    JOL (Java Object Layout) — used by particle-v2-mem's HistoryFootprintBench
                    to measure deep retained size of ParticleHistory variants via
                    GraphLayout.parseInstance(history).totalSize() (R1.2).

                    Profile-scoped (not top-level) because no test under src/test/java
                    references it; placing it here keeps it off the default `mvn test`
                    classpath. This deviates from jmh-core/jmh-generator-annprocess (top-
                    level) — those moved up because pre-existing benches outside this spec
                    depend on them at compile time. JOL has no such constraint.

                    Coordinates: the spec text says "1.17" but the latest GA on Maven
                    Central is 0.17 (https://repo1.maven.org/maven2/org/openjdk/jol/jol-core/);
                    using 0.17.
                -->
                <dependency>
                    <groupId>org.openjdk.jol</groupId>
                    <artifactId>jol-core</artifactId>
                    <version>0.17</version>
                    <scope>test</scope>
                </dependency>
            </dependencies>

            <build>
                <plugins>
                    <!-- Register src/test/jmh as an extra test source root. -->
                    <plugin>
                        <groupId>org.codehaus.mojo</groupId>
                        <artifactId>build-helper-maven-plugin</artifactId>
                        <version>3.6.0</version>
                        <executions>
                            <execution>
                                <id>add-jmh-test-source</id>
                                <phase>generate-test-sources</phase>
                                <goals><goal>add-test-source</goal></goals>
                                <configuration>
                                    <sources>
                                        <source>src/test/jmh</source>
                                    </sources>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>

                    <!-- Enable JMH annotation processor for test compilation only. -->
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-compiler-plugin</artifactId>
                        <version>3.13.0</version>
                        <executions>
                            <execution>
                                <id>default-testCompile</id>
                                <phase>test-compile</phase>
                                <goals><goal>testCompile</goal></goals>
                                <configuration>
                                    <source>${java.version}</source>
                                    <target>${java.version}</target>
                                    <release>${maven.compiler.release}</release>
                                    <parameters>true</parameters>
                                    <proc>full</proc>
                                    <compilerArgs>
                                        <arg>--enable-preview</arg>
                                        <arg>--add-modules</arg>
                                        <arg>${vector.module}</arg>
                                    </compilerArgs>
                                    <annotationProcessorPaths>
                                        <path>
                                            <groupId>org.openjdk.jmh</groupId>
                                            <artifactId>jmh-generator-annprocess</artifactId>
                                            <version>1.37</version>
                                        </path>
                                    </annotationProcessorPaths>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>

                    <!--
                        Bind `exec:exec` to run org.openjdk.jmh.Main with the benchmark regex.
                        `exec:exec` (not `exec:java`) spawns a separate `java` process so that
                        JMH's own `@Fork` mechanism can fork again with the correct classpath.
                        `exec:java` runs in-process and the forked VM cannot see the JMH jars.
                    -->
                    <plugin>
                        <groupId>org.codehaus.mojo</groupId>
                        <artifactId>exec-maven-plugin</artifactId>
                        <version>3.1.0</version>
                        <executions>
                            <execution>
                                <id>jmh-benchmark</id>
                                <phase>integration-test</phase>
                                <goals><goal>exec</goal></goals>
                                <configuration>
                                    <executable>java</executable>
                                    <classpathScope>test</classpathScope>
                                    <arguments>
                                        <argument>--enable-preview</argument>
                                        <argument>--add-modules=${vector.module}</argument>
                                        <argument>-classpath</argument>
                                        <classpath/>
                                        <argument>org.openjdk.jmh.Main</argument>
                                        <argument>${benchmarks}</argument>
                                    </arguments>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
